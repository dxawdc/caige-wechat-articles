# 资料维护与更新

版本：v1.0.0｜更新：2026-09-15

## 当前物料

每篇文章或独立工具保留一个当前资料根目录、一份完整 README、一份物料清单及一个下载 ZIP。仓库首页和 `catalog.json` 是发现入口，子目录 README 是实际使用说明。已经对外分享的最新版本路径继续保留；新资料优先采用稳定目录名。

更新直接维护当前文件。校验新材料后清理被替代的旧文件与旧 ZIP，不另建 history、backup 或 N 份版本文件。未改变的有效代码、原始数据、图表及第三方资源保留原文件名；删除前核查脚本、文档、索引和清单依赖。历史由 Git 提交追溯，不改写历史。

只保留最新物料与可复现性同时成立：基础和进阶脚本是不同功能时一起保留；公开汇总与私有原始输入的边界须写在 README 中。不同文章依赖的公共文件可以各自打包，以保证独立下载后可用。

## 一次更新的步骤

1. 修改对应资料及 README；在 `catalog.json` 登记当前路径、版本、清单名、ZIP 名称和摘要。新增资料同步补齐首页入口。
2. 检查公开范围，排除私有数据、会话、草稿标识、凭据、临时输出和未发表全文。`.gitignore` 只是辅助，不能代替内容审查。
3. 运行对应案例所需的验证。只改目录与说明时核对依赖、链接、清单和包一致性，无需重新抓取线上数据或执行全部分析。
4. 在仓库根目录运行 `python tools/repository/refresh.py`，刷新全部当前清单、确定性 ZIP 和下载索引；只从公开资料目录打包，不从本地私人完整包复制。
5. 运行 `python tools/repository/validate.py`。检查通过后审阅 `git diff --stat`、`git diff --check` 及具体变化，再按任务授权提交推送。

清单以 `files[].path` 记录相对资料根目录路径，并含 SHA-256 和字节数；清单不列自身，ZIP 包含清单。刷新清单时保留原有数据口径、articleId、entrypoint 等字段。ZIP 根目录与文件名去掉 `.zip` 后一致，排序与归档时间固定，使相同源文件得到相同包字节。

校验器检查目录覆盖、缺失文件、SHA-256、同源 ZIP、下载包唯一性、README 本地链接、HTML 静态资源与 Python 语法。它不验证远程网址可用性、不执行文章代码，也不证明统计结论正确。GitHub 同库链接按本地路径检查；原始代码示例中的占位输入按各篇说明提供。

不要给仓库统一安装所有文章依赖。文章环境分别记录；中文字体、Chrome/Kaleido、客户端会话等按各篇运行说明准备。

## 发布边界

本地修改、Git 提交、GitHub 推送和公众号发表分别确认。维护此资料库不自动更新公众号草稿、发送消息或发表文章；不要把公网仓库当私人文件备份。
