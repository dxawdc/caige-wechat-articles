# 按参考文章修订代码字号

版本：v1.0.4
更新时间：2026-09-15
状态：原草稿已更新；真实微信阅读页的浏览器验收通过；手机端待确认

用户反馈上一版手机字体仍偏大，并提供已确认正常的[50例文章](https://mp.weixin.qq.com/s/aVKgtU50MSGQ6iMgJh5EiQ)。实际打开参考阅读页后确认：53个代码块中，code层可为14px，但每行都有span[leaf]，其11px字号保留，高亮子节点继承为11px。旧的两篇结构缺少该整行字号层。前版仅增加普通标签字号与text-size-adjust未解决用户手机问题，不再视为有效修复证据。

本版逐行采用code > span[leaf] > 高亮片段的结构，明确leaf为11px，保留现有实际语言、高亮颜色、代码内容、空行与横滑设置。源码与图片内容不变。此结构来自已确认正常文章的实际DOM；不将结构差异直接断言为所有微信客户端字号问题的唯一原因。

本地和API回读HTML在1280、390、360px检查通过；另外模拟code回到14px、普通分词字号被移除的情况，leaf层仍使文字保持11px。此模拟是针对参考DOM的回归用例，不是平台内部解析器。

本次还通过登录浏览器打开两篇已有的微信临时阅读链接，以390px手机尺寸核对：所有代码实际文字为11px，全部行保留leaf，高亮、源码和横向滚动通过；截图已查看。未发送预览消息或发表文章。浏览器手机模拟仍不等于用户手机微信最终确认。

版本说明：复用50例文章的逐行字号层，增加真实微信阅读页验收；保留旧版本。
