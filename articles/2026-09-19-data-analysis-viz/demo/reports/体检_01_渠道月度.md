# 数据体检报告

## 一、基本信息
- 规模：102 行 × 6 列
- 完全重复行：6
- 唯一键候选：未找到
- 时间字段 `月份`：2025-10-01T00:00:00 → 2026-09-01T00:00:00

## 二、字段清单

| 字段 | 类型 | 角色 | 唯一值 | 缺失率 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `月份` | str | 时间 | 12 | 0.0% |  |
| `渠道` | str | 分类维度 | 9 | 0.0% | 内容社区(14)、线下活动(14)、应用商店(13) |
| `曝光量` | float64 | 度量 | 93 | 3.9% | 中位数 6.846e+04，P95 8.351e+05，偏度 1.25 |
| `点击量` | int64 | 度量 | 95 | 0.0% | 中位数 4066，P95 1.512e+04，偏度 1.05 |
| `新增用户` | int64 | 度量 | 86 | 0.0% | 中位数 217，P95 624.4，偏度 0.62 |
| `收入` | float64 | 度量 | 96 | 0.0% | 中位数 1.167e+04，P95 5.273e+04，偏度 1.04 |

## 三、质量问题
- 未找到唯一键（含字段组合）：可能粒度不纯（同一行代表多种对象），或本来就允许多行代表同一实体
- 存在 6 行完全重复（占 5.88%），需先判断是脏数据还是真实重复事件，再决定去重
- 字段 `渠道` 疑似同义异写：`信息流` 与 `信息流广告`，需统一为标准写法后再分组
- 字段 `曝光量` 偏度 1.25（高度偏态），汇报时用中位数与分位数，不要只用均值
- 字段 `点击量` 偏度 1.05（高度偏态），汇报时用中位数与分位数，不要只用均值
- 字段 `收入` 偏度 1.04（高度偏态），汇报时用中位数与分位数，不要只用均值

## 四、图表建议
- `月份` × `曝光量` → 折线图（单系列）或多系列折线（分组）
- `渠道` × `曝光量` → 柱状图（类别 ≤ 12，标签短）
- `曝光量` 与 `点击量` → 散点图 + 回归线（两个连续数值变量）
- 多维场景 → 气泡图（第三个度量映射到面积，分类映射到颜色），变量不超过 4 个
- 度量 ≥ 4 → 先算相关性热力矩阵，再选代表变量深入
- `曝光量` 右偏长尾（偏度 1.25）→ 箱线图 + 对数轴，或分位数柱状图
- `点击量` 右偏长尾（偏度 1.05）→ 箱线图 + 对数轴，或分位数柱状图
- `收入` 右偏长尾（偏度 1.04）→ 箱线图 + 对数轴，或分位数柱状图

## 五、下一步

1. 确认口径：每个关键字段写出自包含定义（分子、分母、人群、时间窗、实体、排除项）。
2. 处理异常值：区分输入错误与真实长尾，记录处置规则与影响行数。
3. 提取统计特征：偏态字段用中位数与分位数，分组后重算关键统计量。
4. 选图：按 `references/04-图表匹配.md` 从分析意图推导图型。