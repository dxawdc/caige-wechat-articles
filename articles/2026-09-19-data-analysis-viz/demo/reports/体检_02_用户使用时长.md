# 数据体检报告

## 一、基本信息
- 规模：12000 行 × 6 列
- 完全重复行：0
- 唯一键候选：用户ID

## 二、字段清单

| 字段 | 类型 | 角色 | 唯一值 | 缺失率 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `用户ID` | str | 标识符 | 12000 | 0.0% |  |
| `平台` | str | 分类维度 | 2 | 0.0% | 安卓(8041)、iOS(3959) |
| `设备档位` | str | 分类维度 | 3 | 0.0% | 中端(5788)、低端(3861)、高端(2351) |
| `单次使用时长秒` | int64 | 度量 | 2054 | 0.0% | 中位数 249，P95 1880，偏度 60.46 |
| `周会话次数` | int64 | 顺序维度 | 19 | 0.0% | 6(1995)、5(1965)、7(1687) |
| `是否付费` | int64 | 分类维度 | 2 | 0.0% | 0(11374)、1(626) |

## 三、质量问题
- 字段 `用户ID` 取值唯一，是标识符，不参与统计与图表维度
- 字段 `单次使用时长秒` 偏度 60.46（高度偏态），汇报时用中位数与分位数，不要只用均值
- 字段 `单次使用时长秒` 最大值是中位数的 21398 倍，属于极端极值：先核对是不是**单位混用或输入错误**（如一部分行用元、一部分用万元，或毫秒写成秒），确认无误后再按真实长尾处理
- 字段 `是否付费` 高度不平衡（最大类占比 94.8%），分组分析时注意小类样本量

## 四、图表建议
- `平台` × `设备档位` × `单次使用时长秒` → **热力矩阵**（分类×分类，用颜色深浅表示数值，比分组柱状图更省空间）
- `单次使用时长秒` 右偏长尾（偏度 60.46）→ 箱线图 + 对数轴，或分位数柱状图
- `单次使用时长秒` 前 10% 贡献 88.8% → 排序条形图 + 累计曲线（帕累托图）

## 五、下一步

1. 确认口径：每个关键字段写出自包含定义（分子、分母、人群、时间窗、实体、排除项）。
2. 处理异常值：区分输入错误与真实长尾，记录处置规则与影响行数。
3. 提取统计特征：偏态字段用中位数与分位数，分组后重算关键统计量。
4. 选图：按 `references/04-图表匹配.md` 从分析意图推导图型。