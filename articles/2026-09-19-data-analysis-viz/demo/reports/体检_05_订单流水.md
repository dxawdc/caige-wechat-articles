# 数据体检报告

## 一、基本信息
- 规模：200000 行 × 6 列
- 完全重复行：0
- 唯一键候选：订单号
- 时间字段 `下单时间`：2026-01-01T00:00:59 → 2026-06-30T23:56:37

## 二、字段清单

| 字段 | 类型 | 角色 | 唯一值 | 缺失率 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `订单号` | str | 标识符 | 200000 | 0.0% |  |
| `用户ID` | str | 标识符 | 43656 | 0.0% |  |
| `下单时间` | str | 时间 | 198704 | 0.0% |  |
| `订单金额` | float64 | 度量 | 80654 | 0.0% | 中位数 134.4，P95 5723，偏度 9.33 |
| `品类` | str | 分类维度 | 7 | 0.0% | 日用百货(52021)、食品生鲜(42286)、服饰鞋包(35868) |
| `来源渠道` | str | 分类维度 | 4 | 0.0% | 自然流量(68044)、推荐(57882)、搜索(44108) |

## 三、质量问题
- 字段 `订单号` 取值唯一，是标识符，不参与统计与图表维度
- 字段 `订单金额` 偏度 9.33（高度偏态），汇报时用中位数与分位数，不要只用均值

## 四、图表建议
- `下单时间` × `订单金额` → 折线图（单系列）或多系列折线（分组）
- `下单时间` × `品类` × `订单金额` → **堆叠面积图 / 100% 堆叠面积图**（同时看总量与构成随时间变化；若要比较中间层大小，改用多条折线或小倍数分面）
- `订单金额` 右偏长尾（偏度 9.33）→ 箱线图 + 对数轴，或分位数柱状图
- `订单金额` 前 10% 贡献 79.9% → 排序条形图 + 累计曲线（帕累托图）

## 五、下一步

1. 确认口径：每个关键字段写出自包含定义（分子、分母、人群、时间窗、实体、排除项）。
2. 处理异常值：区分输入错误与真实长尾，记录处置规则与影响行数。
3. 提取统计特征：偏态字段用中位数与分位数，分组后重算关键统计量。
4. 选图：按 `references/04-图表匹配.md` 从分析意图推导图型。